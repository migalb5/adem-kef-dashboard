

# return total for last available month plus variation vs. selected reference year/month (for comparison)
de_current_total <- function (db_conn) {

}

# return total for last available month plus variation vs. selected reference year/month (for comparison)
de_res_mesure <- function (db_conn) {

}

# ----------------------------------------------------------------------------

# return current or given year, and previous 2 years
de_res <- function (year = lubridate::year(Sys.Date()), db_conn) {

}

# return current or given year, and previous 2 years
de_cho_comp <- function (year = lubridate::year(Sys.Date()), db_conn) {

}

# return values for last available month, or given year/month
de_per_genre <- function (year = lubridate::year(Sys.Date()), month = lubridate::month(Sys.Date()), db_conn) {

}

# return values for last available month, or given year/month
de_per_age_group <- function (year = lubridate::year(Sys.Date()), month = lubridate::month(Sys.Date()), db_conn) {

}

# return values for last available month, or given year/month
de_per_education_level <- function (year = lubridate::year(Sys.Date()), month = lubridate::month(Sys.Date()), db_conn) {

}

# return values for last available month, or given year/month
de_per_reg_dur <- function (year = lubridate::year(Sys.Date()), month = lubridate::month(Sys.Date()), db_conn) {

}

# return values for last available month, and same month of previous year, plus % variation between both
de_per_nat <- function (year = lubridate::year(Sys.Date()), month = lubridate::month(Sys.Date()), db_conn) {

}

# ----------------------------------------------------------------------------
# ----------------------------------------------------------------------------
# ----------------------------------------------------------------------------

# return total for last available month plus variation vs. selected reference year/month (for comparison)
dei_current_total <- function (db_conn) {

}

# return total for last available month plus variation vs. selected reference year/month (for comparison)
dei_IPA <- function (db_conn) {

}

# ----------------------------------------------------------------------------

# return values for last available month, or given year/month
dei_per_res <- function (year = lubridate::year(Sys.Date()), month = lubridate::month(Sys.Date()), db_conn) {

}

# return values for top 10 of last available month, or given year/month
dei_per_mesure <- function (year = lubridate::year(Sys.Date()), month = lubridate::month(Sys.Date()), db_conn) {

}
